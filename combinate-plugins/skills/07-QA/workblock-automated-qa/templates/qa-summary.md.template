# QA Report — {project} · {workblock}

**Verdict:** {PASS|WARN|FAIL}

**Run:** {startedAt} → {finishedAt} ({duration_min} min)
**Base URL:** {baseUrl}
**Mode:** {dev|support}

## Suite verdicts

| Suite | Result | Detail |
|---|---|---|
| console | {verdict} | {N} errors, {M} failed requests across {pages} pages |
| meta | {verdict} | {N}/{M} pages pass full metadata audit |
| nav | {verdict} | {pass}/{total} links resolve |
| axe | {verdict} | {crit} critical, {ser} serious, {mod} moderate violations |
| screenshots | {verdict} | {N} captures ({viewports}) |
| lighthouse | {verdict} | {N}/{M} pages pass thresholds (mobile + desktop) |
| w3c | {verdict} | {N} HTML errors across {M} pages |
| playwright | {verdict} | {pass}/{total} tests pass{N skipped if any} |

## Issues to action

1. **{suite} / {page}** — {one-line title}
   {detail; help URL if axe}

## Critical-path pages

- **{page}**: {verdict}

## Files

- HTML report: `{outputDir}/report.html`
- Raw artefacts: `{outputDir}/`

---

_Generated by `workblock-automated-qa` skill._
